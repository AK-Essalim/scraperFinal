let axios,cheerio,db;_571‍.x([["getHTML",()=>getHTML],["getTwitterFollowers",()=>getTwitterFollowers],["getInstagramFollowers",()=>getInstagramFollowers],["getInstagramCount",()=>getInstagramCount],["getTwitterCount",()=>getTwitterCount],["runCron",()=>runCron]]);_571‍.w("axios",[["default",["axios"],function(v){axios=v}]]);_571‍.w("cheerio",[["default",["cheerio"],function(v){cheerio=v}]]);_571‍.w("./db",[["default",["db"],function(v){db=v}]]);_571‍.w("./lib/cron");


       async function getHTML(url) {
  const { data: html } = await axios.get(url);
  return html;
}


       async function getTwitterFollowers(html) {
  //load up cheerio
  const $ = cheerio.load(html);
  const span = $('[data-nav="followers"] .ProfileNav-value');
  return parseInt(span.data("count"));
}

       async function getInstagramFollowers(html) {
  const $ = cheerio.load(html);
  const dataInString = $('script[type="application/ld+json"]').html();
  const pageObject = JSON.parse(dataInString);
  const instaCount = parseInt(
    pageObject.mainEntityofPage.interactionStatistic.userInteractionCount
  );

  return instaCount;
}
       async function getInstagramCount() {
  const instaHTML = await getHTML("https://www.instagram.com/narultra/");
  const instaCount = await getInstagramFollowers(instaHTML);
  return instaCount;
}

       async function getTwitterCount() {
  const twHTML = await getHTML("https://twitter.com/AK_Essalim");
  const twCount = await getTwitterFollowers(twHTML);
  return twCount;
}
       async function runCron() {
  const [iCount, tCount] = await Promise.all([
    getInstagramCount(),
    getTwitterCount()
  ]);
  db.get("twitter")
    .push({
      date: Date.now(),
      count: tCount
    })
    .write();

  db.get("instagram")
    .push({
      date: Date.now(),
      count: iCount
    })
    .write();
  res.json({ iCount, tCount });
  console.log("done");
}
